name: manufacturing_defect_platform
version: "1.0.0"
config-version: 2

profile: manufacturing_defect_platform

model-paths:
  - models
macro-paths:
  - macros

models:
  manufacturing_defect_platform:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
